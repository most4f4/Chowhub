// This defines a global app-wide state for the current logged-in user.

// Stores user info (decoded from JWT)

// Allows any component to access user data like role, restaurantId, etc.

// Used with <AuthProvider> around your app

// export const AuthContext = createContext(null);

// Used with:

// const { user } = useContext(AuthContext);
